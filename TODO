some way to sort the validations so they're faster - year first
some yield occurrences methods
check for presence of build_* methods on all validations
cov
move builders into folders
change builds to statics that receive partitioned arrays
test for to_yaml with count
